import React from 'react'
import { Navbar } from '../Navbar'

export const About = () => {
    return (
        <div>
            <Navbar />
            We currently have the best Inventory of cars in the state of New Jersey, so pretty shitty.
        </div>
    )
}